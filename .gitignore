# Dart / Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/

# Generated Drift files — regenerate with:
#   dart run build_runner build --delete-conflicting-outputs
lib/db/app_db.g.dart

# Backups and editor noise
*.bak
*.autobak*
*.orig
__pycache__/
.DS_Store
Thumbs.db
.atlas_backups/
.claude/

# Platform build outputs
android/.gradle/
android/app/debug/
android/app/profile/
android/app/release/
android/local.properties
ios/Flutter/.last_build_id
ios/Pods/
macos/Pods/
windows/flutter/ephemeral/
linux/flutter/ephemeral/

# Local secrets — NEVER commit
.env
.env.*
.local/
*_secrets.json
shopify_*_secret*
shopify_*_token*
secrets.yaml
telegram_token.txt

# Local demo/runtime scratch
.demo_appdata/

# Project Ops Capsule local evidence/outboxes
.project/runs/
.project/atlas_outbox/
.project/boh_outbox/
.project/verification/

# Local database backups and launch logs — NEVER commit (contain real personal data)
.project/db_backups/
.project/*.log

# Installer output (built artifacts, not source)
dist/
atlas-windows-*.zip
*_project_bundle.zip
