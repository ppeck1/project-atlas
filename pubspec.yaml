name: project_atlas
description: "Project Atlas — local-first personal project management."
publish_to: 'none'
version: 1.3.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  drift: ^2.31.0
  drift_flutter: ^0.2.8
  path: ^1.9.1
  path_provider: ^2.1.5
  go_router: ^17.1.0
  http: ^1.2.0
  flutter_markdown: ^0.7.0
  mime: ^2.0.0
  file_picker: ^8.0.0
  url_launcher: ^6.3.0
  flutter_html: ^3.0.0
  archive: ^3.6.0
  xml: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.31.0
  build_runner: ^2.11.1
  sqlite3: ^2.9.4
  path_provider_platform_interface: ^2.1.0
  plugin_platform_interface: ^2.1.0

flutter:
  uses-material-design: true
